# 项目交接说明

## 1. 项目目标

这是一个给美团 AI 产品经理 AI coding 考察准备的前端 Demo，产品主题是：

- `慢病用药小管家`
- 核心形态：`主应用 + 右侧微信专属 Agent`
- 目标是演示完整闭环：
  - 多模态 AI 录入
  - 用药打卡
  - 库存扣减
  - 库存预警
  - 续方/复诊导流
  - 子女关怀视角
  - 体征日记
  - 微信 Agent 陪护提醒

当前重点不是后端真实接入，而是把产品逻辑、AI 场景和交互闭环做完整。

---

## 2. 当前技术栈

- `React 18`
- `TypeScript`
- `Vite`
- 单页应用，无后端
- 数据全部在前端内存中维护

说明：

- 已经**移除了 localStorage 持久化**
- 页面刷新后会恢复到初始状态，便于演示

---

## 3. 本地运行方式

在项目根目录执行：

```powershell
npm install
npm run dev
```

生产构建：

```powershell
npm run build
```

---

## 4. GitHub Pages 部署方式

当前已经改成适配 GitHub Pages 的模式。

### 关键配置

- `vite.config.ts`
- `vite.config.js`

构建时使用：

```ts
base: "/meituan_coding/"
```

这样构建产物资源路径会正确指向：

```text
/meituan_coding/assets/...
```

### 自动部署工作流

文件：

- `.github/workflows/deploy-pages.yml`

作用：

- push 到 `main` 后自动 `npm ci`
- 自动 `npm run build`
- 自动上传 `dist`
- 自动部署到 GitHub Pages

### Pages 配置要求

GitHub 仓库设置里应当使用：

- `Settings -> Pages -> Source -> GitHub Actions`

不要再切回：

- `Deploy from a branch`

否则会重新发布源码根目录的 `index.html`，导致线上白屏。

---

## 5. 核心代码位置

### 主要业务逻辑

- `src/App.tsx`

这里包含：

- 页面主结构
- 药品数据模型
- 打卡逻辑
- 库存扣减
- Agent 消息生成
- 提醒/二次提醒/漏服逻辑
- 智能药箱导入逻辑
- 体征日记逻辑
- GitHub Pages 无关的大部分产品逻辑

### 样式

- `src/styles.css`

这里包含：

- 主应用布局
- Agent 右侧悬浮侧边栏
- 聊天气泡
- 摘要卡片
- 智能药箱列表滚动

### 部署配置

- `vite.config.ts`
- `vite.config.js`
- `.github/workflows/deploy-pages.yml`

---

## 6. 当前产品/交互状态

### 6.1 顶层结构

主应用有三个标签页：

- `今日打卡`
- `智能药箱`
- `体征日记`

顶部有身份切换：

- `患者本人`
- `子女关怀`

右侧是微信 Agent 侧边栏：

- 展开时显示完整会话
- 收起时只显示右侧悬浮按钮

### 6.2 默认演示状态

- 默认演示时间：`07:50`
- 刷新后回到初始状态
- 默认有几种预置药品和一条体征记录

### 6.3 打卡与库存

- 打卡后库存按单次剂量扣减
- 漏服不会扣减库存
- 剩余天数 `<= 3` 会出现库存预警

### 6.4 Agent 逻辑

Agent 现在已经不是静态展示，具备以下已实现行为：

- 按时间点生成提醒
- 支持“稍后提醒我”
- 二次提醒文案与首次提醒不同
- 二次提醒才出现“漏服”
- 超过提醒窗口 2 小时后可升级为子女提醒
- “让 Agent 跟进”会打开侧边栏并生成针对当前药品的跟进消息
- “一键续方”会打开美团买药网页链接（Demo 模拟）
- “在线复诊”会打开健康问诊网页链接（Demo 模拟）

### 6.5 智能药箱

支持两类入库：

- 历史订单导入
- 多模态 AI 识别确认后入库

当前已做的关键修正：

- 重复导入同一药品时，不再新增重复条目
- 会合并到现有药品库存
- Agent 系统消息会清楚说明：
  - 原库存多少
  - 本次补充多少
  - 现库存多少

### 6.6 库存列表滚动

智能药箱右侧库存列表已经加了最大高度：

- 超过一定数量后，只在列表内部滚动
- 不会再把整页无限拉长

---

## 7. 已处理过的关键问题

本轮迭代已经处理过这些问题，后续不要重复踩坑：

1. **GitHub Pages 白屏**
   - 原因：部署了源码根目录，不是 `dist`
   - 原因：未设置正确 `base`
   - 现已修复

2. **Agent 消息顺序错误**
   - 现已按完整时间戳稳定排序

3. **Agent 头像左右位置错误**
   - 现已修正为：Agent 左侧，用户右侧

4. **库存提醒误触发服药打卡**
   - 库存提醒与服药提醒已解耦
   - 库存卡只负责续方/风险提示

5. **“稍后提醒我”无真实二次提醒**
   - 现已支持二次提醒时间推进

6. **第一次提醒就允许“漏服”**
   - 现已调整为：至少二次提醒时才出现“漏服”

7. **Agent 收起后占位和遮挡问题**
   - 现已改为悬浮侧边栏 + 收起按钮

8. **Agent 区域滚动条过多**
   - 现已收敛为主要消息区滚动

9. **Agent 底部摘要只显示“今日早药”**
   - 现已改为显示：
     - 早药
     - 中药
     - 晚药
     - 睡前药

10. **智能药箱重复导入导致无限新增卡片**
    - 现已改为同药合并库存

---

## 8. 当前仍值得继续优化的点

如果下个会话继续打磨，优先建议看这些：

1. **Agent 摘要卡可读性**
   - 当前已基本可用
   - 还可以继续微调排版和字号

2. **“让 Agent 跟进”按钮文案**
   - 现在已经有真实行为
   - 但文案可以进一步变得更直白，比如：
     - `交给 Agent 处理`
     - `让 Agent 提醒这款药`

3. **库存合并后的主界面高亮反馈**
   - 当前是消息提示
   - 可考虑让被补库存的药卡短暂高亮

4. **GitHub Actions 的 Node 20 deprecation warnings**
   - 当前不影响部署
   - 是 GitHub Actions 运行时升级的提示
   - 后续可统一升级/兼容 Node 24

5. **真实多模态与真实外部服务**
   - 当前仍为 Demo/mock 级
   - 后续如果需要可接 API

---

## 9. 若下个会话要快速上手，建议先做什么

建议按这个顺序读项目：

1. 看 `HANDOFF.md`
2. 看 `src/App.tsx`
3. 看 `src/styles.css`
4. 本地运行 `npm run dev`
5. 手动演示以下时间点：
   - `07:50`
   - `08:35`
   - `09:05`
   - `10:45`
   - `11:10`
   - `19:00`
   - `21:40`

这样最快理解当前所有关键交互。

---

## 10. 当前部署结论

当前项目已经可以：

- 本地正常运行
- 本地正常构建
- GitHub Pages 正常访问

只要后续继续保持：

- `Source = GitHub Actions`
- push 到 `main`

就会自动重新部署。
